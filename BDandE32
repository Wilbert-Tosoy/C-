#include <stdio.h>

void BDandE32() {
    int x;
    printf("Enter a divisor: ");
    scanf("%d", &x);
    for (int i = 1; i <= 100; i++) {
        if (i % x == 3) {
            printf("%d\n", i);
        }
    }
}

int main(void) {
    BDandE32();
    return 0;
}
