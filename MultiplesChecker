#include <stdio.h>

void multiplesChecker() {
    int values[2];

    for (int i = 0; i < 2; i++) {
        printf("Enter the value of index %d: ", i + 1);
        scanf("%d", &values[i]);
    }

    if (values[1] % values[0] == 0 ) {
        printf("Multiple of %d", values[0]);
    } else {
        printf("Not multiple of %d", values[0]);
    }
}

int main(void) {
    multiplesChecker();
    return 0;
}
