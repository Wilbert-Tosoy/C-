#include <stdio.h>

void BDandE35() {
    int x, y;
    printf("Enter the first number: ");
    scanf("%d", &x);
    printf("Enter the second number: ");
    scanf("%d", &y);
    if (x > y) {
        printf("Decscending order!");
    } else {
        printf("Ascending Order!");
    }
}

int main(void) {
    BDandE35();
    return 0;
}
