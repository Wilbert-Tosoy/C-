#include <stdio.h>

void shift() {
    int x, y;
    printf("Enter a number: ");
    scanf("%d", &x);
    x<<=2;
    y = x;
    printf("The left shifted data is: %d", y);
}
int main(void) {
    shift();
    return 0;
}
